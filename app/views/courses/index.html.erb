<h3 style="margin-bottom: 50px;">
  <div class="container">
    <div class="row">
      <h3 style="padding-left: 15px; color: #333"> <%= t('.title') %> </h3>
      <% @courses.each do |_course| %>
          <%= render partial: "course", locals: { course: _course } %>
      <% end %>
    </div>
  </div>

  <%= #render partial: "course", collection: @feature_courses
  %>
</h3>